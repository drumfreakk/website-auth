authcode permissions (enforce)
Logging
daemon to (add) remove codes
sessid is linked to session, when you login its linked to account serverside, authcode stored seperately
authcode timescale
clientside hashing+salting (first try now ish, make it secure later (with https) and try to crack it in the mean time?)
add sessid support (on first request add sessid, sent with every req., link on login)
ipv html php files that automatically check for sessid?
user permission levels
Add OAuth2
